import { PropertyMatchPipe } from './property-match.pipe';

describe('PropertyMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
