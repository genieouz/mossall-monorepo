import { DateRangerFilterPipe } from './date-ranger-filter.pipe';

describe('DateRangerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateRangerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
